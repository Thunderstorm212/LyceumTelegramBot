from lib import Colors, StatusBar


class Statuses:
    def __init__(self, inp):
        if inp == 1:
            print(Colors("Build access", "#0f0"))
        if inp == 1:
            print(Colors("Runed status remover", "#0f0"))
